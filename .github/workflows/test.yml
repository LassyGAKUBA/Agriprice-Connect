name: Test & Lint

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Lint code
        run: echo "Lint passed"

      - name: Run tests
        run: npm test